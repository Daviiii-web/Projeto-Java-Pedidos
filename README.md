# 🛒 Order System – Projeto Java

Projeto desenvolvido para praticar conceitos fundamentais de **POO**, composição entre classes, uso de **enum**, listas com `List`/`ArrayList` e manipulação de datas utilizando `LocalDate` e `LocalDateTime`.

O programa permite registrar um cliente, adicionar produtos ao pedido informando nome, preço e quantidade, e no final exibe um resumo completo com cada item, subtotal e o valor total do pedido.

## 🔧 Tecnologias utilizadas
- Java 17+
- Programação Orientada a Objetos (POO)
- `LocalDate` e `LocalDateTime`
- `Enum` para status do pedido
- `List` e `ArrayList`
- Entrada de dados com `Scanner`

## 📚 Aprendizados
- Composição entre classes (`Order`, `OrderItem`, `Product`, `Client`)
- Organização de modelos e responsabilidades
- Cálculo de subtotal e total do pedido
- Formatação e parsing de datas no Java moderno
- Aplicação de boas práticas como imutabilidade e programação orientada a interfaces

## ▶️ Como executar
Basta compilar o projeto e executar a classe `Main`.  
O programa solicitará os dados do cliente, status do pedido e itens, e então exibirá um resumo completo.

---

Projeto criado para estudo e aprimoramento da lógica e organização em Java.
